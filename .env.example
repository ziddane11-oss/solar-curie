# ============================================================
# Server-side only (절대 NEXT_PUBLIC_ 붙이지 말 것)
# ============================================================

# Supabase - Service Role Key (서버에서만 사용, RLS 우회)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI API (AI 해설 생성, 서버에서만 호출)
OPENAI_API_KEY=sk-your-openai-api-key

# Admin API 보호 (최소 32자 랜덤 문자열 권장)
ADMIN_TOKEN=your-secret-admin-token

# 카카오톡 채널 딥링크 (서버에서 랜딩 페이지에 주입)
KAKAO_CHANNEL_URL=https://pf.kakao.com/_your_channel

# 유튜브 채널 (서버에서 랜딩 페이지에 주입)
YOUTUBE_CHANNEL_URL=https://youtube.com/@your_channel

# ============================================================
# Client-side (브라우저에 노출됨 - 민감 정보 금지)
# ============================================================

# 유튜브 채널 URL (클라이언트 컴포넌트에서 사용 시)
NEXT_PUBLIC_YOUTUBE_CHANNEL_URL=https://youtube.com/@your_channel

# ============================================================
# 주의사항
# ============================================================
# - SUPABASE_SERVICE_ROLE_KEY는 절대 NEXT_PUBLIC_으로 만들지 마세요
# - ADMIN_TOKEN은 Preview/Production 환경에서 다른 값 사용 권장
# - Preview 환경에서는 RSS 수집 크론을 등록하지 마세요 (중복 데이터 방지)
